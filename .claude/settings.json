{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport sys, json\ndata = json.load(sys.stdin)\ncmd = data.get('tool_input', {}).get('command', '')\nif 'git add' in cmd and 'src/main/frontend' in cmd:\n    print('{\\\"decision\\\":\\\"block\\\",\\\"reason\\\":\\\"Submodul-Pointer-Commit verboten. Nur explizit wenn du es anweist.\\\"}')\n\""
          }
        ]
      }
    ]
  }
}